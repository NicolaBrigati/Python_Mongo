Go to: https://www.mongodb.com/try/download/community > Install > Create the folder in c: > c:\data\db {if needed}
Go to Mongo Compass: https://www.mongodb.com/try/download/shell > GUI to access to online and local DB

From Compass > new connection to: mongodb://localhost:27017
Create db > pymongo; Create Collections > users

From PyCharm > Install package pyMongo

Try MongoEngine > is a Python library that provides an Object-Document Mapping (ODM) system for working with MongoDB, a NoSQL database. Similar to an Object-Relational Mapping (ORM) tool like SQLAlchemy for relational databases, MongoEngine helps developers work with MongoDB data using Python objects and classes instead of writing raw MongoDB queries.